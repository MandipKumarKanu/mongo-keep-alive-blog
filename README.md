# 🛡️ MongoDB Keep Alive (GitHub Actions)

This project prevents **MongoDB Atlas free-tier cluster** from being auto-deleted due to inactivity.  
It uses **GitHub Actions** to automatically ping database once a week.
